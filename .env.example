# RunSafe n8n Environment Variables
# Copy this file to .env and update with your actual values

# ==================== REQUIRED VARIABLES ====================

# n8n encryption key - MUST be changed for production!
# Generate with: openssl rand -hex 32
N8N_ENCRYPTION_KEY=CHANGE_ME_USE_OPENSSL_RAND_HEX_32

# RunSafe tenant key - Provided during onboarding
# Contact support@runsafe.ai to obtain your key
RUNSAFE_TENANT_KEY=YOUR_TENANT_KEY_HERE

# Policy enforcement mode
# Options: monitor (log only) | enforce (block/redact violations)
# Start with 'monitor' for pilot, switch to 'enforce' after validation
POLICY_MODE=monitor

# ==================== OPTIONAL VARIABLES ====================

# Database configuration (defaults shown)
# Only change if using external PostgreSQL
# DB_HOST=postgres
# DB_PORT=5432
# DB_NAME=audit
# DB_USER=postgres
# DB_PASSWORD=postgres

# Service ports (defaults shown)
# Change if ports conflict with existing services
# N8N_PORT=5678
# GATEWAY_PORT=8080
# DASHBOARD_PORT=8081

# n8n configuration
# N8N_HOST=runsafe-n8n.local
# N8N_PROTOCOL=http
# GENERIC_TIMEZONE=Europe/Berlin

# Allowed AI providers (comma-separated)
# Add additional providers as needed
# ALLOWLIST_HOSTS=api.openai.com,api.anthropic.com

# ==================== PRODUCTION VARIABLES ====================

# SSL/TLS Configuration (for production deployments)
# SSL_ENABLED=false
# SSL_CERT_PATH=/path/to/cert.pem
# SSL_KEY_PATH=/path/to/key.pem

# Backup configuration
# BACKUP_ENABLED=false
# BACKUP_SCHEDULE=0 2 * * *
# BACKUP_RETENTION_DAYS=30

# Monitoring
# ENABLE_METRICS=false
# METRICS_PORT=9090

# ==================== DEBUGGING ====================

# Enable debug logging (set to true for troubleshooting)
# DEBUG=false
# LOG_LEVEL=info