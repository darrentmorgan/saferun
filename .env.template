\# RunSafe n8n Environment Variables

# n8n encryption key (generate securely, e.g., openssl rand -hex 16)
N8N_ENCRYPTION_KEY=changeme123

# Tenant key issued by RunSafe
RUNSAFE_TENANT_KEY=changeme_tenant

# RunSafe policy mode: monitor (log only) or enforce (block/redact)
POLICY_MODE=monitor
